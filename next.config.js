/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/TimeoutBCN',
  assetPrefix: '/TimeoutBCN/',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
